#!/bin/sh

# Passing the following parameters to mergetool:
#  local base remote merge_result

#alocal=$1
#base=$2
#remote=$3
#result=$4

'/cygdrive/c/Program Files (x86)/KDiff3/kdiff3.exe' -m "$(cygpath -wla "$2")" "$(cygpath -wla "$1")" "$(cygpath -wla "$3")" -o "$(cygpath -wla "$4")"
